import 'package:flutter/material.dart';
import 'package:runn/pages/tabs/tab.dart';

TabWidget statsTab = TabWidget(
  appBar: null,
  body: Builder(builder: (context) {
    return Column();
  }),
  fab: null,
);
